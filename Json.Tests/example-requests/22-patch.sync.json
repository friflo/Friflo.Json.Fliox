{
    "type": "sync",
    "tasks": [
    {
      "task":       "patch",
      "info":       "patch article member .name (article need to be present)",
      "container":  "articles",
      "patches": {
        "new-article": {
          "patches": [
            {
              "op":"replace",
              "path": ".name",
              "value": "patched Name"
            }
          ]
        }
      }
    }
  ]
}