{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$comment": "Generated by: https://github.com/friflo/Friflo.Json.Fliox#schema",
    "definitions": {
        "SyncRequestTask": {
            "discriminator": "task",
            "oneOf": [
                { "$ref": "#/definitions/CreateEntities" },
                { "$ref": "#/definitions/UpsertEntities" },
                { "$ref": "#/definitions/ReadEntities" },
                { "$ref": "#/definitions/QueryEntities" },
                { "$ref": "#/definitions/AggregateEntities" },
                { "$ref": "#/definitions/PatchEntities" },
                { "$ref": "#/definitions/DeleteEntities" },
                { "$ref": "#/definitions/SendMessage" },
                { "$ref": "#/definitions/SendCommand" },
                { "$ref": "#/definitions/CloseCursors" },
                { "$ref": "#/definitions/SubscribeChanges" },
                { "$ref": "#/definitions/SubscribeMessage" },
                { "$ref": "#/definitions/ReserveKeys" }
            ],
            "properties": {
                "task": { "enum": ["create", "upsert", "read", "query", "aggregate", "patch", "delete", "message", "command", "closeCursors", "subscribeChanges", "subscribeMessage", "reserveKeys"], "description": "task type" },
                "info": {  }
            },
            "required": [
                "task"
            ],
            "additionalProperties": true
        },
        "CreateEntities": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncRequestTask" },
            "description": "Create the given <b>entities</b> in the specified <b>container</b>",
            "properties": {
                "task":          { "enum": ["create"] },
                "info":          {  },
                "container":     { "type": "string", "description": "container name the <b>entities</b> are created" },
                "reservedToken": { "oneOf": [{ "$ref": "./Standard.json#/definitions/Guid" }, {"type": "null"}] },
                "keyName":       { "type": ["string", "null"], "description": "name of the primary key property in <b>entities</b>" },
                "entities":      { "type": "array", "items": {  }, "description": "the <b>entities</b> which are created in the specified <b>container</b>" }
            },
            "required": [
                "task",
                "container",
                "entities"
            ],
            "additionalProperties": false
        },
        "UpsertEntities": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncRequestTask" },
            "description": "Upsert the given <b>entities</b> in the specified <b>container</b>",
            "properties": {
                "task":      { "enum": ["upsert"] },
                "info":      {  },
                "container": { "type": "string", "description": "container name the <b>entities</b> are upserted - created or updated" },
                "keyName":   { "type": ["string", "null"], "description": "name of the primary key property in <b>entities</b>" },
                "entities":  { "type": "array", "items": {  }, "description": "the <b>entities</b> which are upserted in the specified <b>container</b>" }
            },
            "required": [
                "task",
                "container",
                "entities"
            ],
            "additionalProperties": false
        },
        "ReadEntities": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncRequestTask" },
            "description": "Read entities by id from the specified <b>container</b> using given list of <b>ids</b><br/>\nTo return also entities referenced by entities listed in <b>ids</b> use <b>references</b>. <br/>\nThis mimic the functionality of a <b>LEFT JOIN</b> in <b>SQL</b>",
            "properties": {
                "task":       { "enum": ["read"] },
                "info":       {  },
                "container":  { "type": "string", "description": "container name" },
                "keyName":    { "type": ["string", "null"], "description": "name of the primary key property of the returned entities" },
                "isIntKey":   { "type": ["boolean", "null"] },
                "ids":        { "type": "array", "items": { "type": "string" }, "description": "list of requested entity <b>ids</b>" },
                "references": { "type": ["array", "null"], "items": { "$ref": "./Friflo.Json.Fliox.Hub.Protocol.Models.json#/definitions/References" }, "description": "used to request the entities referenced by properties of a read task result" }
            },
            "required": [
                "task",
                "container",
                "ids"
            ],
            "additionalProperties": false
        },
        "QueryEntities": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncRequestTask" },
            "description": "Query entities from the given <b>container</b> using a <b>filter</b><br/>\nTo return entities referenced by fields of the query result use <b>references</b>",
            "properties": {
                "task":       { "enum": ["query"] },
                "info":       {  },
                "container":  { "type": "string", "description": "container name" },
                "keyName":    { "type": ["string", "null"], "description": "name of the primary key property of the returned entities" },
                "isIntKey":   { "type": ["boolean", "null"] },
                "filterTree": { "description": "query filter as JSON tree. <br/>\nIs used in favour of <b>filter</b> as its serialization is more performant" },
                "filter":     { "type": ["string", "null"], "description": "query filter as a <a href='https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/operators/lambda-expressions'>Lambda expression</a>\nreturning a boolean value. E.g.<br/><code>o => o.name == 'Smartphone'</code>\nif <b>filterTree</b> is assigned it has priority" },
                "references": { "type": ["array", "null"], "items": { "$ref": "./Friflo.Json.Fliox.Hub.Protocol.Models.json#/definitions/References" }, "description": "used to request the entities referenced by properties of the query task result" },
                "limit":      { "oneOf": [{ "$ref": "./Standard.json#/definitions/int32" }, {"type": "null"}], "description": "limit the result set to the given number" },
                "maxCount":   { "oneOf": [{ "$ref": "./Standard.json#/definitions/int32" }, {"type": "null"}], "description": "execute a cursor request with the specified <b>maxCount</b> number of entities in the result." },
                "cursor":     { "type": ["string", "null"], "description": "specify the <b>cursor</b> of a previous cursor request" }
            },
            "required": [
                "task",
                "container"
            ],
            "additionalProperties": false
        },
        "AggregateEntities": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncRequestTask" },
            "description": "Aggregate - count - entities from the given <b>container</b> using a <b>filter</b><br/>",
            "properties": {
                "task":       { "enum": ["aggregate"] },
                "info":       {  },
                "container":  { "type": "string", "description": "container name" },
                "type":       { "$ref": "#/definitions/AggregateType", "description": "aggregation type - e.g. count" },
                "filterTree": { "description": "aggregation filter as JSON tree. <br/>\nIs used in favour of <b>filter</b> as its serialization is more performant" },
                "filter":     { "type": ["string", "null"], "description": "aggregation filter as a <a href='https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/operators/lambda-expressions'>Lambda expression</a>\nreturning a boolean value. E.g.<br/><code>o => o.name == 'Smartphone'</code>" }
            },
            "required": [
                "task",
                "container",
                "type"
            ],
            "additionalProperties": false
        },
        "AggregateType": {
            "enum": [
                "count"
            ],
            "descriptions": {
                "count": "count entities"
            }

        },
        "PatchEntities": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncRequestTask" },
            "description": "Patch entities by id in the given <b>container</b><br/>\nEach <b>EntityPatch</b> in <b>patches</b> contains a set of <b>patches</b> for each entity.",
            "properties": {
                "task":      { "enum": ["patch"] },
                "info":      {  },
                "container": { "type": "string", "description": "container name" },
                "keyName":   { "type": ["string", "null"], "description": "name of the primary key property of the entity <b>patches</b>" },
                "patches":   { "type": "array", "items": { "$ref": "#/definitions/EntityPatch" }, "description": "list of patches for each entity" }
            },
            "required": [
                "task",
                "container",
                "patches"
            ],
            "additionalProperties": false
        },
        "EntityPatch": {
            "type": "object",
            "description": "Contains the <b>patches</b> applied to an entity. Used by <b>PatchEntities</b>",
            "properties": {
                "id":      { "type": "string" },
                "patches": { "type": "array", "items": { "$ref": "./Friflo.Json.Fliox.Transform.json#/definitions/JsonPatch" }, "description": "list of patches applied to an entity" }
            },
            "required": [
                "id",
                "patches"
            ],
            "additionalProperties": false
        },
        "DeleteEntities": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncRequestTask" },
            "description": "Delete entities by id in the given <b>container</b><br/>\nThe entities which will be deleted are listed in <b>ids</b>",
            "properties": {
                "task":      { "enum": ["delete"] },
                "info":      {  },
                "container": { "type": "string", "description": "container name" },
                "ids":       { "type": ["array", "null"], "items": { "type": "string" }, "description": "list of <b>ids</b> requested for deletion" },
                "all":       { "type": ["boolean", "null"], "description": "if true all entities in the specified <b>container</b> are deleted" }
            },
            "required": [
                "task",
                "container"
            ],
            "additionalProperties": false
        },
        "SendMessage": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncMessageTask" },
            "description": "Send a database message with the given <b>param</b>. <br/>\nIn case <b>users</b> or <b>clients</b> is set the Hub forward\nthe message as an event only to the given <b>users</b> or <b>clients</b>.",
            "properties": {
                "task":    { "enum": ["message"] },
                "info":    {  },
                "name":    { "type": "string", "description": "command / message name" },
                "param":   { "description": "command / message parameter. Can be null or absent" },
                "users":   { "type": ["array", "null"], "items": { "type": "string" }, "description": "if set the Hub forward the message as an event only to given <b>users</b>" },
                "clients": { "type": ["array", "null"], "items": { "type": "string" }, "description": "if set the Hub forward the message as an event only to given <b>clients</b>" },
                "groups":  { "type": ["array", "null"], "items": { "type": "string" }, "description": "if set the Hub forward the message as an event only to given <b>groups</b>" }
            },
            "required": [
                "task",
                "name"
            ],
            "additionalProperties": false
        },
        "SyncMessageTask": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncRequestTask" },
            "isAbstract": true,
            "description": "Used as base type for <b>SendMessage</b> or <b>SendCommand</b> to specify the command / message\n<b>name</b> and <b>param</b>. <br/>\nIn case <b>users</b> or <b>clients</b> is set the Hub forward the message as an event only to the\ngiven <b>users</b> or <b>clients</b>.",
            "properties": {
                "info":    {  },
                "name":    { "type": "string", "description": "command / message name" },
                "param":   { "description": "command / message parameter. Can be null or absent" },
                "users":   { "type": ["array", "null"], "items": { "type": "string" }, "description": "if set the Hub forward the message as an event only to given <b>users</b>" },
                "clients": { "type": ["array", "null"], "items": { "type": "string" }, "description": "if set the Hub forward the message as an event only to given <b>clients</b>" },
                "groups":  { "type": ["array", "null"], "items": { "type": "string" }, "description": "if set the Hub forward the message as an event only to given <b>groups</b>" }
            },
            "required": [
                "name"
            ],
            "additionalProperties": false
        },
        "SendCommand": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncMessageTask" },
            "description": "Send a database command with the given <b>param</b>. <br/>\nIn case <b>users</b> or <b>clients</b> is set the Hub forward\nthe message as an event only to the given <b>users</b> or <b>clients</b>.",
            "properties": {
                "task":    { "enum": ["command"] },
                "info":    {  },
                "name":    { "type": "string", "description": "command / message name" },
                "param":   { "description": "command / message parameter. Can be null or absent" },
                "users":   { "type": ["array", "null"], "items": { "type": "string" }, "description": "if set the Hub forward the message as an event only to given <b>users</b>" },
                "clients": { "type": ["array", "null"], "items": { "type": "string" }, "description": "if set the Hub forward the message as an event only to given <b>clients</b>" },
                "groups":  { "type": ["array", "null"], "items": { "type": "string" }, "description": "if set the Hub forward the message as an event only to given <b>groups</b>" }
            },
            "required": [
                "task",
                "name"
            ],
            "additionalProperties": false
        },
        "CloseCursors": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncRequestTask" },
            "description": "Close the <b>cursors</b> of the given <b>container</b>",
            "properties": {
                "task":      { "enum": ["closeCursors"] },
                "info":      {  },
                "container": { "type": "string", "description": "container name" },
                "cursors":   { "type": ["array", "null"], "items": { "type": "string" }, "description": "list of <b>cursors</b>" }
            },
            "required": [
                "task",
                "container"
            ],
            "additionalProperties": false
        },
        "SubscribeChanges": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncRequestTask" },
            "description": "Subscribe to specific <b>changes</b> of the specified <b>container</b> using the given <b>filter</b>",
            "properties": {
                "task":      { "enum": ["subscribeChanges"] },
                "info":      {  },
                "container": { "type": "string", "description": "container name" },
                "changes":   { "type": "array", "items": { "$ref": "#/definitions/EntityChange" }, "description": "subscribe to entity <b>changes</b> of the given <b>container</b>" },
                "filter":    { "type": ["string", "null"], "description": "subscription filter as a <a href='https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/operators/lambda-expressions'>Lambda expression</a> (infix notation)\nreturning a boolean value. E.g. <c>o.name == 'Smartphone'</c>" }
            },
            "required": [
                "task",
                "container",
                "changes"
            ],
            "additionalProperties": false
        },
        "EntityChange": {
            "enum": [
                "create",
                "upsert",
                "patch",
                "delete"
            ],
            "descriptions": {
                "create": "filter change events of created entities.",
                "upsert": "filter change events of upserted entities.",
                "patch":  "filter change events of entity patches.",
                "delete": "filter change events of deleted entities."
            }

        },
        "SubscribeMessage": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncRequestTask" },
            "description": "Subscribe to commands and messages sent to a database by their <b>name</b><br/>\nUnsubscribe by setting <b>remove</b> to true",
            "properties": {
                "task":   { "enum": ["subscribeMessage"] },
                "info":   {  },
                "name":   { "type": "string", "description": "subscribe a specific message: 'std.Echo', multiple messages by prefix: 'std.*', all messages: '*'" },
                "remove": { "type": ["boolean", "null"], "description": "if true a previous added subscription is removed. Otherwise added" }
            },
            "required": [
                "task",
                "name"
            ],
            "additionalProperties": false
        },
        "ReserveKeys": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncRequestTask" },
            "description": "WIP",
            "properties": {
                "task":      { "enum": ["reserveKeys"] },
                "info":      {  },
                "container": { "type": "string" },
                "count":     { "$ref": "./Standard.json#/definitions/int32" }
            },
            "required": [
                "task",
                "container",
                "count"
            ],
            "additionalProperties": false
        },
        "SyncTaskResult": {
            "discriminator": "task",
            "oneOf": [
                { "$ref": "#/definitions/CreateEntitiesResult" },
                { "$ref": "#/definitions/UpsertEntitiesResult" },
                { "$ref": "#/definitions/ReadEntitiesResult" },
                { "$ref": "#/definitions/QueryEntitiesResult" },
                { "$ref": "#/definitions/AggregateEntitiesResult" },
                { "$ref": "#/definitions/PatchEntitiesResult" },
                { "$ref": "#/definitions/DeleteEntitiesResult" },
                { "$ref": "#/definitions/SendMessageResult" },
                { "$ref": "#/definitions/SendCommandResult" },
                { "$ref": "#/definitions/CloseCursorsResult" },
                { "$ref": "#/definitions/SubscribeChangesResult" },
                { "$ref": "#/definitions/SubscribeMessageResult" },
                { "$ref": "#/definitions/ReserveKeysResult" },
                { "$ref": "#/definitions/TaskErrorResult" }
            ],
            "properties": {
                "task": { "enum": ["create", "upsert", "read", "query", "aggregate", "patch", "delete", "message", "command", "closeCursors", "subscribeChanges", "subscribeMessage", "reserveKeys", "error"], "description": "task result type" }
            },
            "required": [
                "task"
            ],
            "additionalProperties": true
        },
        "CreateEntitiesResult": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncTaskResult" },
            "description": "Result of a <b>CreateEntities</b> task",
            "properties": {
                "task":   { "enum": ["create"] },
                "errors": { "type": ["array", "null"], "items": { "$ref": "./Friflo.Json.Fliox.Hub.Protocol.Models.json#/definitions/EntityError" }, "description": "list of entity errors failed to create" }
            },
            "required": [
                "task"
            ],
            "additionalProperties": false
        },
        "UpsertEntitiesResult": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncTaskResult" },
            "description": "Result of a <b>UpsertEntities</b> task",
            "properties": {
                "task":   { "enum": ["upsert"] },
                "errors": { "type": ["array", "null"], "items": { "$ref": "./Friflo.Json.Fliox.Hub.Protocol.Models.json#/definitions/EntityError" }, "description": "list of entity errors failed to upsert" }
            },
            "required": [
                "task"
            ],
            "additionalProperties": false
        },
        "ReadEntitiesResult": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncTaskResult" },
            "description": "Result of a <b>ReadEntities</b> task",
            "properties": {
                "task":       { "enum": ["read"] },
                "references": { "type": ["array", "null"], "items": { "$ref": "./Friflo.Json.Fliox.Hub.Protocol.Models.json#/definitions/ReferencesResult" } }
            },
            "required": [
                "task"
            ],
            "additionalProperties": false
        },
        "QueryEntitiesResult": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncTaskResult" },
            "description": "Result of a <b>QueryEntities</b> task",
            "properties": {
                "task":       { "enum": ["query"] },
                "container":  { "type": ["string", "null"], "description": "container name - not utilized by Protocol" },
                "cursor":     { "type": ["string", "null"] },
                "count":      { "oneOf": [{ "$ref": "./Standard.json#/definitions/int32" }, {"type": "null"}], "description": "number of <b>ids</b> - not utilized by Protocol" },
                "ids":        { "type": "array", "items": { "type": "string" } },
                "references": { "type": ["array", "null"], "items": { "$ref": "./Friflo.Json.Fliox.Hub.Protocol.Models.json#/definitions/ReferencesResult" } }
            },
            "required": [
                "task",
                "ids"
            ],
            "additionalProperties": false
        },
        "AggregateEntitiesResult": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncTaskResult" },
            "description": "Result of a <b>AggregateEntities</b> task",
            "properties": {
                "task":      { "enum": ["aggregate"] },
                "container": { "type": ["string", "null"], "description": "container name - not utilized by Protocol" },
                "value":     { "oneOf": [{ "$ref": "./Standard.json#/definitions/double" }, {"type": "null"}] }
            },
            "required": [
                "task"
            ],
            "additionalProperties": false
        },
        "PatchEntitiesResult": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncTaskResult" },
            "description": "Result of a <b>PatchEntities</b> task",
            "properties": {
                "task":   { "enum": ["patch"] },
                "errors": { "type": ["array", "null"], "items": { "$ref": "./Friflo.Json.Fliox.Hub.Protocol.Models.json#/definitions/EntityError" }, "description": "list of entity errors failed to patch" }
            },
            "required": [
                "task"
            ],
            "additionalProperties": false
        },
        "DeleteEntitiesResult": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncTaskResult" },
            "description": "Result of a <b>DeleteEntities</b> task",
            "properties": {
                "task":   { "enum": ["delete"] },
                "errors": { "type": ["array", "null"], "items": { "$ref": "./Friflo.Json.Fliox.Hub.Protocol.Models.json#/definitions/EntityError" }, "description": "list of entity errors failed to delete" }
            },
            "required": [
                "task"
            ],
            "additionalProperties": false
        },
        "SendMessageResult": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncMessageResult" },
            "description": "Result of a <b>SendMessage</b> task",
            "properties": {
                "task": { "enum": ["message"] }
            },
            "required": [
                "task"
            ],
            "additionalProperties": false
        },
        "SyncMessageResult": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncTaskResult" },
            "isAbstract": true,
            "properties": {

            },
            "additionalProperties": false
        },
        "SendCommandResult": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncMessageResult" },
            "description": "Result of a <b>SendCommand</b> task",
            "properties": {
                "task":   { "enum": ["command"] },
                "result": {  }
            },
            "required": [
                "task"
            ],
            "additionalProperties": false
        },
        "CloseCursorsResult": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncTaskResult" },
            "description": "Result of a <b>CloseCursors</b> task",
            "properties": {
                "task":  { "enum": ["closeCursors"] },
                "count": { "$ref": "./Standard.json#/definitions/int32" }
            },
            "required": [
                "task",
                "count"
            ],
            "additionalProperties": false
        },
        "SubscribeChangesResult": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncTaskResult" },
            "description": "Result of a <b>SubscribeChanges</b> task",
            "properties": {
                "task": { "enum": ["subscribeChanges"] }
            },
            "required": [
                "task"
            ],
            "additionalProperties": false
        },
        "SubscribeMessageResult": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncTaskResult" },
            "description": "Result of a <b>SubscribeMessage</b> task",
            "properties": {
                "task": { "enum": ["subscribeMessage"] }
            },
            "required": [
                "task"
            ],
            "additionalProperties": false
        },
        "ReserveKeysResult": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncTaskResult" },
            "description": "WIP",
            "properties": {
                "task": { "enum": ["reserveKeys"] },
                "keys": { "oneOf": [{ "$ref": "#/definitions/ReservedKeys" }, {"type": "null"}] }
            },
            "required": [
                "task"
            ],
            "additionalProperties": false
        },
        "ReservedKeys": {
            "type": "object",
            "isStruct": true,
            "description": "WIP",
            "properties": {
                "start": { "$ref": "./Standard.json#/definitions/int64" },
                "count": { "$ref": "./Standard.json#/definitions/int32" },
                "token": { "$ref": "./Standard.json#/definitions/Guid" }
            },
            "required": [
                "start",
                "count",
                "token"
            ],
            "additionalProperties": false
        },
        "TaskErrorResult": {
            "type": "object",
            "extends": { "$ref": "#/definitions/SyncTaskResult" },
            "description": "A <b>TaskErrorResult</b> is returned in case execution of a <b>SyncRequestTask</b> failed",
            "properties": {
                "task":       { "enum": ["error"] },
                "type":       { "$ref": "#/definitions/TaskErrorResultType", "description": "task error type" },
                "message":    { "type": ["string", "null"], "description": "task error details" },
                "stacktrace": { "type": ["string", "null"], "description": "stacktrace in case the error <b>type</b> is a <b>UnhandledException</b>" }
            },
            "required": [
                "task",
                "type"
            ],
            "additionalProperties": false
        },
        "TaskErrorResultType": {
            "enum": [
                "None",
                "UnhandledException",
                "DatabaseError",
                "FilterError",
                "ValidationError",
                "CommandError",
                "InvalidTask",
                "NotImplemented",
                "PermissionDenied",
                "SyncError"
            ],
            "descriptions": {
                "UnhandledException": "Unhandled exception while executing a task.<br/>\nmaps to HTTP status: 500",
                "DatabaseError":      "General database error while task execution.<br/>\nE.g. the access is currently not available or accessing a missing table.<br/>\nmaps to HTTP status: 500",
                "FilterError":        "Invalid query filter   <br/> maps to HTTP status: 400",
                "ValidationError":    "Schema validation of an entity failed  <br/> maps to HTTP status: 400",
                "CommandError":       "Execution of message / command failed caused by invalid input  <br/>maps to HTTP status: 400",
                "InvalidTask":        "Invalid task. E.g. by using an invalid task parameter  <br/>maps to HTTP status: 400",
                "NotImplemented":     "database message / command not implemented     <br/>maps to HTTP status: 501",
                "PermissionDenied":   "task execution not authorized  <br/>maps to HTTP status: 403",
                "SyncError":          "The entire <b>SyncRequest</b> containing a task failed  <br/>maps to HTTP status: 500"
            }

        }
    }
}

