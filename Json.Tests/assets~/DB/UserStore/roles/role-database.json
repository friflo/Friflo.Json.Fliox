{
    "id": "role-database",
    "description": "Allow the specified operations on a database container and change subscriptions to these containers",
    "rights": [
        { "type": "database", "containers": {
            "articles": { "operations": ["read", "upsert"], "subscribeChanges": ["upsert"] }
        }}
    ]
}