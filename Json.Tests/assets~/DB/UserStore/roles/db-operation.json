{
    "id": "db-operation",
    "description": "Allow access of specified operations to a database container and change subscriptions to these containers",
    "rights": [
        {
            "type": "operation",
            "containers": {
                "articles": { "operations": ["read", "upsert"], "subscribeChanges": ["upsert"] }
            }
        }
    ]
}