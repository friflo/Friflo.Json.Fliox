{
  "msg": "sync",
  "tasks": [
    {
      "task":       "query",
      "container":  "articles",
      "filter":     { "op": "true" }
    }
  ],
  "info": [
    "query all articles",
    "'filter': { 'op': 'true' } - a predicate function - means return all"
  ],
  "user":   "{{user}}",
  "token":  "{{token}}"
}