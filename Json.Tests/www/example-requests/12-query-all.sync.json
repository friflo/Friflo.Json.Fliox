{
  "msg": "sync",
  "tasks": [
    {
      "task":       "query",
      "container":  "articles",
      "filterJson": { "op": "true" }
    }
  ],
  "info": [
    "query all articles",
    "'filter': { 'op': 'true' } - a predicate function - means return all"
  ]
}