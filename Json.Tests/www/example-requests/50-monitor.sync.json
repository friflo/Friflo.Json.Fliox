{
    "msg": "sync",
    "database": "monitor",
    "tasks": [
      {
        "task":       "query",
        "container":  "clients",
        "filterLinq": "true",
        "filter":     { "op": "true" }
      },
      {
        "task":       "query",
        "container":  "users",
        "filterLinq": "true",
        "filter":     { "op": "true" }
      }
    ],
    "info": [
      "monitor: query all clients & users",
      "list request, task & event statistics"
    ],
    "user":   "{{user}}",
    "token":  "{{token}}"
  }