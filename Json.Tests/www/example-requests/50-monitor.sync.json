{
    "msg": "sync",
    "database": "monitor",
    "tasks": [
      {
        "task":       "query",
        "container":  "clients",
        "filter":     { "op": "true" }
      },
      {
        "task":       "query",
        "container":  "users",
        "filter":     { "op": "true" }
      }
    ],
    "info": [
      "monitor: query all clients & users",
      "list request, task & event statistics"
    ],
    "user":   "{{user}}",
    "token":  "{{token}}"
  }