{
    "type": "sync",
    "user": "{{user}}",
    "token":"{{token}}",  
    "tasks": [{
      "task":     "subscribeMessage",
      "name":     "*",
      "remove":   false
    }
  ],
  "info": "Filter all messages matching to 'name'. (Websocket only)"
}