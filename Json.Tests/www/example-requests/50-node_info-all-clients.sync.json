{
  "msg": "sync",
  "database": "node_info",
  "tasks": [
    {
      "task":       "query",
      "container":  "clients",
      "filterLinq": "true",
      "filter":     { "op": "true" }
    }
  ],
  "info": ["query all clients"],
  "user":   "{{user}}",
  "token":  "{{token}}"
}