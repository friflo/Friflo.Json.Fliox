{
  "msg": "sync",
  "database": "user_db",
  "tasks": [
    { "task": "query", "container": "permissions", "filter":{ "op": "true" }},
    { "task": "query", "container": "roles",       "filter":{ "op": "true" }}
  ],
  "info": [
    "query all permissions & roles from user_db"
  ],
  "user":   "{{user}}",
  "token":  "{{token}}"
}