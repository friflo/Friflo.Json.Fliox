{
  "msg": "sync",
  "tasks": [
    {
      "task":  "command",
      "name":  "Echo",
      "value": "echo value"
    }
  ],
  "info": [
      "Send a command to host.",
      "When handled by host it returns a result.",
      "'Echo' is a standard host command.",
      "It simply echos the given value."
  ],
  "user":   "{{user}}",
  "token":  "{{token}}"
}