{
  "msg": "sync",
  "database": "user_db",
  "tasks": [
    {
        "task": "read",
        "container":  "permissions",
        "reads": [{"ids": ["poc-admin"]}]
    },
    {
        "task": "read",
        "container":  "roles",
        "reads": [{"ids": ["db-allow"]}]
    }
  ],
  "info": [
    "query all permissions & roles from user_db"
  ],
  "user":   "{{user}}",
  "token":  "{{token}}"
}