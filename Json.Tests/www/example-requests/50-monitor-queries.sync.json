{
  "msg": "sync",
  "database": "monitor",
  "tasks": [
    { "task": "query", "container": "users",    "filter": {"op": "true"} },
    { "task": "query", "container": "histories","filter": {"op": "true"} }
  ],
  "info": [
    "monitor: query all users & histories",
    "list request & task statistics"
  ],
  "user":   "{{user}}",
  "token":  "{{token}}"
}