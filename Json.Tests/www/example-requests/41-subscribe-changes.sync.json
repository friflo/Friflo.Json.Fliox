{
  "type": "sync",
  "tasks": [
    {
      "task":       "subscribeChanges",
      "container":  "articles", 
      "changes": ["create", "upsert", "delete", "patch"]
    }
  ],
  "info": "subscribe to database changes. (Websocket only)",
  "user": "{{user}}",
  "token":"{{token}}"
}