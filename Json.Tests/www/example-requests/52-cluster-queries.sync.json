{
  "msg": "sync",
  "database": "cluster",
  "tasks": [
    { "task": "query", "container": "catalogs", "filter":{ "op": "true" }}
  ],
  "info": [
    "provide infos about hosted databases",
    "query all catalogs from cluster"
  ],
  "user":   "{{user}}",
  "token":  "{{token}}"
}