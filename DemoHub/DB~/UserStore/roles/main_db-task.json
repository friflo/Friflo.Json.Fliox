{
    "id": "main_db-task",
    "description": "Allow execution of the specified task types. E.g. 'read' enables reads on all containers.",
    "rights": [
        { "type": "task", "database": "main_db", "types": ["read"] }
    ]
}