{
    "id": "main_db-operation",
    "description": "Allow the given database operations on the given containers and subscribing to change operations",
    "rights": [
        {
            "type": "operation",
            "containers": {
                "articles": { "operations": ["read", "upsert"], "subscribeChanges": ["upsert"] }
            }
        }
    ]
}